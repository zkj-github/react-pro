{
  "name": "monorepo-test",
  "version": "1.0.0",
  "private": true,
  "description": "pnpm monorepo",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "esbuild": "^0.28.0"
  }
}
